using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spawnNextObject : MonoBehaviour {


  //  public endlessModeSpawner es;
  //  public GameObject mc;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    void SpawnNext()
    {
       // int index = Random.Range(0, es.spawnnableObjects.Length);
    // GameObject gObj =  (GameObject) Instantiate(es.spawnnableObjects[index], new Vector3(transform.position.x,transform.position.y+5,transform.position.z),transform.rotation);
    }
}
